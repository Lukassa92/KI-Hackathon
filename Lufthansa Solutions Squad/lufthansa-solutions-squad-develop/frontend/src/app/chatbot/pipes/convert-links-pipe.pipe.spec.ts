import { ConvertLinksPipePipe } from './convert-links-pipe.pipe';

describe('ConvertLinksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertLinksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
